rayos-fusion.sh:

The main script, it downloads the lighting data and calls 
the python parser/uploader

parsea_kml.py:
The real program, it recieves a kml as a parameter, parses it and insert 
into fusion tables using oauth 

the rest of files:
Libraries given by Google to intereact with fusion-tables in python language
